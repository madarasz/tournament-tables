name: Tests

on:
  pull_request:
    branches: ['*']
  push:
    branches: [main, master]

jobs:
  phpunit:
    name: PHPUnit Tests (PHP 8.5)
    runs-on: ubuntu-22.04
    timeout-minutes: 15

    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: root
          MYSQL_DATABASE: tournament_tables
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: pdo, pdo_mysql, json, mbstring
          coverage: none

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress

      - name: Validate Composer
        run: composer validate

      - name: Check platform requirements
        run: composer check-platform-reqs

      - name: Create database config
        run: |
          cat > config/database.php << 'EOF'
          <?php
          declare(strict_types=1);
          return [
              'host' => '127.0.0.1',
              'database' => 'tournament_tables',
              'username' => 'root',
              'password' => 'root',
              'charset' => 'utf8mb4',
          ];
          EOF

      - name: Run migrations
        run: php bin/migrate.php

      - name: Seed terrain types
        run: php bin/seed-terrain-types.php

      - name: Lint PHP files
        run: find src tests bin public config -name '*.php' -print0 | xargs -0 -n1 php -l

      - name: Run ALL PHPUnit tests
        run: vendor/bin/phpunit --testsuite unit,integration,performance,contract,e2e
